// 숫자 문자열과 영단어
// replace
// 1. 문자열 메서드
// 2. 첫번째 인자의 데이터를 두번째 인자의 데이터로 변경
// 3. 앞에있는 데이터 딱 하나만 변경된다

// replaceAll
// 첫번째 인자를 두번째 인자로 모두 다 바꿔 준다

// while - 조건에 맞을 때까지 반복한다
// g 명령어는 전역 검색을 의미한다



// const numbers = ["zero", "one","two","three","four",
//                  "five","six","seven","eight","nine"]

// function solution(s){
// 	numbers.forEach((str,i) => {
//     s = s.split(str).join(i)
//     console.log(s)
//   })
//   return Number(s)
// }


// solution("one4seveneight")


// 정규표현식
// /(슬래시)열고 / 닫고 슬래시 안에 검증하는 문자열을 추가한다

// s = s.replace(/zero/g, 0)
// s = s.replace(/one/g, 1)
// s = s.replace(/two/g, 1)
// s = s.replace(/three/g, 1)
// s = s.replace(/four/g, 1)
// s = s.replace(/five/g, 1)
// s = s.replace(/six/g, 1)
// s = s.replace(/seven/g, 1)
// s = s.replace(/eight/g, 1)
// s = s.replace(/nine/g, 1)

// console.log(Number(s))


  
// regex 기능
// regExp